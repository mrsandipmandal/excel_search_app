python -m venv env  (Create Virtual Environment)
env\Scripts\activate (Active Virtual Environment)
pip install flask pandas openpyxl (Install Required Packages pandas and openpyxl)
pip freeze > requirements.txt  (Requirements dependencies save)
pip install -r requirements.txt (dependencies install)
python app.py (Run Your Flask App)
